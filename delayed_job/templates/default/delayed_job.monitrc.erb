check process delayed_job with pidfile <%= @dir %>/shared/pids/delayed_job.pid
  start program = "/bin/sh -c 'cd <%= @dir %>/current; /usr/bin/env PATH=/usr/local/bin:$PATH RAILS_ENV=<%= @env %> <%= @dir %>/current/bin/delayed_job start'" with timeout 60 seconds
  stop program = "/bin/sh -c 'cd <%= @dir %>/current; /usr/bin/env PATH=/usr/local/bin:$PATH RAILS_ENV=<%= @env %> <%= @dir %>/current/bin/delayed_job stop'" with timeout 60 seconds
